---
zuul:
  prefix: /services
#  routes:
#    work:
#      path: /api/**
#      url: http://localhost:8080/person
---
eureka:
  client:
    serviceUrl:
      defaultZone:
    enabled: false
---
spring:
  profiles:
    active: default
  application:
    name: cloud-zulu-gateway
  cloud:
    config:
      uri:
      enabled: false
